DATABASE_URL=postgresql+asyncpg://munshi:munshi@db:5432/munshi
JWT_SECRET=change-me-in-production
JWT_ACCESS_EXPIRE_MINUTES=30
JWT_REFRESH_EXPIRE_DAYS=7

# Set to true to seed a demo tenant (customers, suppliers, employees, sales,
# purchases, cashbook, salary/advance) on API startup. Idempotent — safe to
# leave on, it skips once the demo tenant exists. See app/db/seed.py.
SEED_DEMO_DATA=false
